{
  "name": "blade-box-arena",
  "version": "0.1.0",
  "private": true,
  "description": "Top-down roguelite arena prototype packaged for browser and Android.",
  "scripts": {
    "serve": "node scripts/dev-server.js",
    "build:web": "node scripts/sync-web.js",
    "cap:sync": "npm run build:web && cap sync android",
    "android:open": "npm run cap:sync && cap open android",
    "android:build": "npm run cap:sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.3",
    "@capacitor/core": "^8.3.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.3"
  }
}
